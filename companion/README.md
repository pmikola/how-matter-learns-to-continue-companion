# How Matter Learns to Continue: v48 reader companion

This local package accompanies the v48 covered and interior PDFs identified by
exact hashes in `evidence/v48-compatibility.json`. It starts from the actual v47
companion and preserves its scientific programs, frozen data and historical evidence
byte for byte. The v47 mathematical checks are retained and executed again. This
copyedited edition introduces no new scientific model.

## Read and return

Open `where-to-return.html` or `where-to-return.md` for the existing eight questions.
Put this extracted `how-matter-learns-to-continue-v48-companion` folder beside
`how-matter-learns-to-continue-v48.pdf` and/or
`how-matter-learns-to-continue-v48-interior.pdf`. The PDF files are delivered
separately. Links name destinations verified in each final PDF; printed and physical
page numbers are also supplied. The HTML guide can verify a locally selected PDF
when your folders differ. Selecting a file does not upload it.

Try `reader-exercise.html` before its separate answer key for the distinction
between a retained decision and confidence conditional on the original count.
`reader-variations.html` adds four optional tasks: a fifteen-unit cap, a zero
terminal response, purchased labels, and a chosen Fisher-metric update. Separate
keys contain analytic answers. The six-task pilot, cards, facilitator rubric and
blank form are prepared materials. **No human-reader trial or outcome is claimed.**
The concealed cards retain their historical v45-versus-v46 source mapping; they
are not relabeled as a test of v48.

## Install once, then run the read-only commands

Use Python 3.11 or later. Extract the ZIP, open a terminal in its top-level folder,
create a virtual environment outside that folder if desired, and install the
supplied `requirements.txt`. For example, in Windows PowerShell:

```text
python -m venv ../reader-v48-venv
../reader-v48-venv/Scripts/python.exe -m pip install -r requirements.txt
../reader-v48-venv/Scripts/python.exe -B verify_companion.py
```

On macOS/Linux use `../reader-v48-venv/bin/python` instead. With that interpreter
active, run:

```text
python -B verify_companion.py
python -B orthogonal-checks-v44.py
python -B connection-checks-v45.py --e3 evidence/e3/analysis/per_class.jsonl
python -B reader-operations-v46.py
python -B continuation-checks-v47.py
python -B reconstruct-w7-v47.py
```

The default commands print JSON reports and do not modify the extracted payload.
Use optional explicit output arguments only when you want to save another report.
The retained v47 scripts use the Python standard library; the inherited suite also uses the
requirements supplied with its historical payload.

## What each command establishes

The unchanged `verify_companion.py` runs 35 portable commands, including two small
Chapter 3 training reproductions in memory. It checks frozen E3 arithmetic and
historical framework records; it does not rerun the original neural-framework
training. The printed framework snippets remain byte-equal to their inherited
versions. Their previous framework execution records retain historical attribution.

The unchanged v44 program runs nine numerical groups, using this extraction's
frozen E3 evidence. `deep-dive.md` and `evidence/v44/` retain their original names,
scientific text, snapshots and provenance. The unchanged v45 program runs eleven
connection groups; the explicit `--e3` command additionally verifies the pinned
thirty-row artifact and recomputes its P06 decomposition. The unchanged v46 program
runs N01-N04: 128 action cases, decision versus confidence, exact conditional and
aggregate continuation, and the two acceleration protocols. These historical
scripts have not been renamed merely because they are shipped with v48.

The retained `continuation-checks-v47.py` runs five groups covering the preserved finite
result and new analytic extensions: rule 150, infinite versus capped storage,
purchased calibration labels, and the Fisher/natural-gradient construction. Its
Fisher bridge starts from neutral probability one half and uses five correctness
labels. At strictly positive optimization time its threshold decision agrees with
the original majority choice and yields the same episode mixture. The fitted
probability is not the posterior probability of the high-correctness orientation.
At zero optimization time the parameter remains tied. The chosen loss, update and
optimization clock are essential assumptions; geometry alone supplies none of them.

The retained `reconstruct-w7-v47.py` supplies an independently implemented deterministic
probe generator using the historical protocol, seed and stable-key construction.
It reconstructs eight balanced 32-site preparations, checks their original saved
commitment, and evolves all thirty named W7 classes through 32 updates with two
transition implementations. It compares the resulting integer counts with the
unchanged archived rows and evaluates frozen predictors. Its JSON report includes
the explicit probe states and agreement checks. The rule-150 diagnostic separates
the external terminal readout from earlier propagation and the excluded perturbed
site. The default command discovers `evidence/e3` beside its own script; an explicit
`--evidence PATH` can name another extracted E3 directory or a companion ZIP.

This reconstruction is stronger than saved-row arithmetic. It does not refit a
predictor, recreate the training families, rerun the registered bootstrap or entire
control campaign, or recover the historical derivation of the class selection.
The original result remains `e3_transfer_null`, with scientific admission false.
Clipping and descriptor/group diagnoses remain post-test work on exposed rows.
The named thirty classes have not become a random sample of all rules.

## Edition binding and historical preservation

`evidence/v48-source-route-inventory.json` hashes the current master, all manifest
chapters/notes and recursively linked static TeX inputs. The current source manifest
and compatibility record are separate from historical evidence. Full manuscript
sources and figures are not included. `reader-guide-bindings.json` binds the eight
questions to the final aux labels, object map and independently resolved locations
in both PDFs; it does not assume a constant cover offset.
The eight scientific source anchors added in v47 must resolve directly to the printed pages
declared by their final labels. A named-page fallback is allowed only for older
guide headings and is disclosed separately in the binding record.

All historical scientific payload bytes remain at their original relative paths.
Current-edition guide/material replacements preserve their previous v47 bytes under
`historical/v47/`, including the old README and bundle manifest. Their old PDF links
and version claims describe that historical delivery. Earlier v40/v44/v45 READMEs,
manifests, source records and receipts keep their original names and dates. The
unchanged legacy verifier's old compatibility field is retained in the current
bundle manifest; use `evidence/v48-compatibility.json` for the current edition.

The v48 packager runs all six top-level commands in a fresh extraction and isolated
virtual environment, compares deterministic results with repository snapshots,
checks historical bytes and the extracted file set, rechecks current source/PDF
hashes and creates the final ZIP exclusively. Its delivery receipt is stored beside
the book at `reviews/publication-v48/validation-companion-v48.json`. Historical
receipts are not relabeled as that fresh run.

This is a local delivery. A public download or purchaser access route remains
unestablished. PDF semantic accessibility, assistive-technology reading order,
physical print proofs and the formation/maintenance of a working apparatus remain
separate obligations; these numerical and packaging checks do not certify them.

## Unchanged historical framework record

The following retained v40-era table and description are checked literally by the
unchanged legacy verifier. Their edition references describe historical receipt
compatibility, not a fresh v48 framework run.

The inherited v34 receipt records CPU execution of all four printed forward examples on 600 saved test inputs with copied parameters:

| Framework | Recorded version | Score dtype | Maximum absolute score error against NumPy |
|---|---|---|---:|
| PyTorch | 2.14.0+cpu | float64 | 1.7763568394002505e-15 |
| TensorFlow | 2.21.0 | float64 | 3.5527136788005009e-15 |
| JAX | 0.10.2 | float64 | 0 |
| Keras | 3.15.1 | float32 | 2.1617272860652292e-06 |

The unchanged execution receipt and its environment lock are included at `reviews/publication-v34/framework-verification.json` and `reviews/publication-v34/environment-lock.txt`. Packaging checks the saved-array hash and verifies that the v40 printed snippets have the receipt's exact hashes. `verify_framework_record.py` rechecks those bindings and this table after extraction. These are inherited execution results; v40 packaging does not execute frameworks. They establish the reported forward-score agreement, not training or gradient equivalence. The portable reader requirements do not install frameworks.

The following second table likewise retains its historical heading and wording
because the unchanged v39 evidence verifier checks it literally. Here "fresh"
means fresh in v39, not newly executed for v48. Its strict failures are preserved.

## Fresh v39 forward precision checks

These are fresh CPU executions with copies of the author's saved 600 test inputs and weights. They are separate from the inherited v34 receipt above. No training or gradient equivalence is claimed.

| Configuration | Framework / backend version | Input / first product / score dtype | Maximum absolute logit error | Strict 1e-11 | Practical 1e-5 |
|---|---|---|---:|---|---|
| torch64 | 2.14.0+cpu / 2.14.0+cpu | torch.float64 / torch.float64 / torch.float64 | 1.7763568e-15 | pass | pass |
| tensorflow64 | 2.21.0 / 2.21.0 | float64 / float64 / float64 | 3.5527137e-15 | pass | pass |
| jax64 | 0.10.2 / 0.10.2 | float64 / float64 / float64 | 0 | pass | pass |
| keras-tensorflow-default | 3.15.1 / 2.21.0 | <dtype: 'float32'> / <dtype: 'float32'> / <dtype: 'float32'> | 2.1617273e-06 | FAIL | pass |
| keras-tensorflow-float64 | 3.15.1 / 2.21.0 | <dtype: 'float64'> / <dtype: 'float64'> / <dtype: 'float64'> | 2.5936205e-07 | FAIL | pass |
| keras-tensorflow-full64 | 3.15.1 / 2.21.0 | <dtype: 'float64'> / <dtype: 'float64'> / <dtype: 'float64'> | 3.5527137e-15 | pass | pass |
| keras-jax-default | 3.15.1 / 0.10.2 | float32 / float32 / float32 | 1.6848901e-06 | FAIL | pass |
| keras-jax-float64 | 3.15.1 / 0.10.2 | float64 / float64 / float64 | 2.5936205e-07 | FAIL | pass |
| keras-jax-full64 | 3.15.1 / 0.10.2 | float64 / float64 / float64 | 0 | pass | pass |
| keras-torch-default | 3.15.1 / 2.14.0+cpu | torch.float32 / torch.float32 / torch.float32 | 1.6945625e-06 | FAIL | pass |
| keras-torch-float64 | 3.15.1 / 2.14.0+cpu | torch.float64 / torch.float32 / torch.float64 | 1.586914e-06 | FAIL | pass |
| keras-torch-full64 | 3.15.1 / 2.14.0+cpu | torch.float64 / torch.float32 / torch.float64 | 1.586914e-06 | FAIL | pass |

Every configuration preserved all 600 threshold classifications. That does not make the scores bit-identical. The strict failures above remain failures even though the separately declared practical comparison passes.

`default` retains the printed Keras input and layer defaults. `float64` sets only the layer policy, retaining the default Input specification. `full64` explicitly sets both. In this installed Keras/PyTorch configuration, the directly probed first matrix product still returned float32 with float64 inputs and weights. This records observed behavior, not a diagnosis of every internal kernel or a claim about every library release.

The exact versions, intermediate probes, snippet hashes, original/final checks and initial warning record are included under `reviews/publication-v39`. `verify_v39_evidence.py` checks record consistency, not fresh framework execution. Framework packages are not installed by the portable requirements. With a separately prepared matching environment, run one read-only configuration with `python -B scripts/verify-framework-precision-v39.py --configuration torch64` (or another listed configuration). Do not run the no-argument production orchestrator inside the frozen reader bundle.
